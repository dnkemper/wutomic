# If using yml to configure plugins, rename this to {artsci_media_wysiwyg}.ckeditor5.yml.
# If using annotations, this file can be removed.
# @see https://www.drupal.org/docs/drupal-apis/plugin-api/annotations-based-plugins
#   For information on using annotations to define plugins.
# @see the CKEditor 5 module's README.md for more details regarding plugin
#   configuration options.
# cSpell:ignore simplebox demobox

artsci_media_wysiwyg_linkStyles:
  ckeditor5:
    plugins: []
    config: []
  drupal:
    label: Link styles
    class: Drupal\artsci_media_wysiwyg\Plugin\CKEditor5Plugin\LinkStyles
    library: false
    admin_library: false
    elements:
      - <a class>
    conditions:
      plugins:
        - ckeditor5_link
artsci_media_wysiwyg_html_embed:
  # Use the provider: property for this plugin to depend on another module.
  # Configuration that will be sent to CKEditor 5 JavaScript plugins.
  ckeditor5:
    plugins:
      - htmlEmbed.HtmlEmbed
    config:
      htmlEmbed:
        showPreviews: true

  # Configuration that will be used directly by Drupal.
  drupal:
    label: iframe embed
    # The library loaded while using the editor.
    library: artsci_media_wysiwyg/htmlEmbed
    # The library loaded when configuring the text format using this plugin.
    admin_library: artsci_media_wysiwyg/admin.htmlEmbed
    toolbar_items:
      htmlEmbed:
        label: iframe Embed
    # If the plugin does not provide elements, set this as
    # `elements: false`
    elements:
      # Note that it necessary for elements to separately provide both the tag
      # (f.e. `<div>`) and the attribute being added to the tag
      # (f.e. `<h2 class="raw-html-embed">`).
      - <div>
      - <div class="raw-html-embed">
    # *Additional configuration properties*
    # conditions: for setting additional criteria that must be met for the
    #   plugin to be active.
    # class: Optional PHP class that makes it possible for the plugin to provide
    #   dynamic values, or a configuration UI.
# artsci_media_wysiwyg_table:
#   ckeditor5:
#     plugins:
#       - table.Table
#     config:
#       table:
#         contentToolbar: []
#   drupal:
#     label: Tables
#     library: artsci_media_wysiwyg/table
#     admin_library: artsci_media_wysiwyg/admin.table
#     toolbar_items:
#       insertTable:
#         label: table
#     elements:
#       - <table>
#       - <tr>
#       - <td>
#       - <th>
#       - <thead>
#       - <tbody>
#       - <caption>
# artsci_media_wysiwyg_html_embed:
#   # Use the provider: property for this plugin to depend on another module.
#   # Configuration that will be sent to CKEditor 5 JavaScript plugins.
#   ckeditor5:
#     plugins:
#       - htmlEmbed.HtmlEmbed
#     config:
#       htmlEmbed:
#         showPreviews: false

#   # Configuration that will be used directly by Drupal.
#   drupal:
#     label: Iframe embed
#     # The library loaded while using the editor.
#     library: artsci_media_wysiwyg/htmlEmbed
#     # The library loaded when configuring the text format using this plugin.
#     admin_library: artsci_media_wysiwyg/admin.htmlEmbed
#     toolbar_items:
#       htmlEmbed:
#         label: Iframe Embed
#     # If the plugin does not provide elements, set this as
#     # `elements: false`
#     elements:
#       # Note that it necessary for elements to separately provide both the tag
#       # (f.e. `<div>`) and the attribute being added to the tag
#       # (f.e. `<h2 class="raw-html-embed">`).
#       - <div>
#       - <div class="raw-html-embed">
#     # *Additional configuration properties*
#     # conditions: for setting additional criteria that must be met for the
#     #   plugin to be active.
#     # class: Optional PHP class that makes it possible for the plugin to provide
#     #   dynamic values, or a configuration UI.
