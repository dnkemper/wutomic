{%
set classes = [
  'add-to-calendar'
]
%}
<span{{attributes.addClass(classes)}}>
  {% if label %}<label class="addtocal__label">{{ label }}</label>:{% endif %}
  <a href="{{ ical }}" download="event.ics">Add to Calendar</a>
{{ addtocal }}